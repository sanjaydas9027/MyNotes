// Extract a specific string from a given sentence
function extractSubstring(sentence, start, end) {
    return sentence.substring(start, end);
}
console.log(extractSubstring("JavaScript programming", 0, 10));